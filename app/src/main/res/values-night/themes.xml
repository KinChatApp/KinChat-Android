<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Tuktak" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">#131218</item>
    </style>

    <!-- 🚀 NEW: Splash Screen Theme (Dark) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#131218</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/Theme.Tuktak</item>
    </style>
</resources>
